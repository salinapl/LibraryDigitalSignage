title: slideshow
icon: 🎞️
fields:
  delay:
    label: Delay Between Slides (Seconds)
    type: number
    min: 10
  tags:
    label: Campaign Categories
    type: tags
    accept: options
    options: query
    query: site.index.pluck('Tags', ',', true)
